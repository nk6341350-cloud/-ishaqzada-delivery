{
  "name": "ishaqzada-delivery-independent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply ishaqzada-delivery --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
